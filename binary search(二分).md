| 函数 | 作用、返回类型 |
| -- | -- |
| `lower_bound`   | 满足 `>= x` 的第一个位置，返回迭代器 |
| `upper_bound`   | 第一个严格大于 `x` 的位置，返回迭代器  |
| `binary_search` | 判断是否存在 `x`，返回`bool`    |
###### 根据 STL 的接口约定，它们接收的区间统一是：
```
[first, last) //即左闭右开
```
##### 手写二分时可以采用双开区间：
